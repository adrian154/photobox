const fs = require("fs/promises");
const path = require("path");

module.exports = class {

    constructor(config) {
        this.config = config;
    }

    save(object) {

    }

    delete(object) {
        
    }

};